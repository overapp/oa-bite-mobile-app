// ignore_for_file: constant_identifier_names

// Enum representing the types of HTTP requests that can be made.
// Each value corresponds to a standard HTTP method.

enum RequestType {
  GET,
  POST,
  PUT,
  PATCH,
  DELETE,
}
